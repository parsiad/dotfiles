[env]
TERM = "xterm-256color"

[font.normal]
family = "SauceCodePro NF"
style = "SemiBold"

[font]
size = {{ if eq .chezmoi.hostname "sagely" }}10{{ else }}12{{ end }}

[general]
import = ["~/.alacritty.theme.toml"]
